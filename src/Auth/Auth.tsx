import { Typography } from "@mui/joy";
import "./Auth.css";

function Auth() {
    return (
        <Typography level="h1" className="auth-text">
            Авторизуйся
        </Typography>
    );
}

export default Auth;
